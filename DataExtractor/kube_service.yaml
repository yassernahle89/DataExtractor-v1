
# ============================================================
# 2. CSV Service
# ============================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: csv-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: csv-service
  template:
    metadata:
      labels:
        app: csv-service
    spec:
      containers:
      - name: csv-service
        image: dokcerregistery.azurecr.io/csv-service:v1.1
        ports:
        - containerPort: 8021
        resources:
          requests:
            cpu: "100m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: csv-service
spec:
  type: ClusterIP
  selector:
    app: csv-service
  ports:
  - port: 8021
    targetPort: 8021

---
# ============================================================
# 3. Excel Service
# ============================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: excel-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: excel-service
  template:
    metadata:
      labels:
        app: excel-service
    spec:
      containers:
      - name: excel-service
        image: dokcerregistery.azurecr.io/excel-service:v1.1
        ports:
        - containerPort: 8022
        resources:
          requests:
            cpu: "100m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: excel-service
spec:
  type: ClusterIP
  selector:
    app: excel-service
  ports:
  - port: 8022
    targetPort: 8022

---
# ============================================================
# 4. Image Service
# ============================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: image-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: image-service
  template:
    metadata:
      labels:
        app: image-service
    spec:
      containers:
      - name: image-service
        image: dokcerregistery.azurecr.io/image-service:v1.1
        ports:
        - containerPort: 8023
        resources:
          requests:
            cpu: "100m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: image-service
spec:
  type: ClusterIP
  selector:
    app: image-service
  ports:
  - port: 8023
    targetPort: 8023

---
# ============================================================
# 5. PDF Service
# ============================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: pdf-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pdf-service
  template:
    metadata:
      labels:
        app: pdf-service
    spec:
      containers:
      - name: pdf-service
        image: dokcerregistery.azurecr.io/pdf-service:v1.1
        ports:
        - containerPort: 8024
        resources:
          requests:
            cpu: "100m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: pdf-service
spec:
  type: ClusterIP
  selector:
    app: pdf-service
  ports:
  - port: 8024
    targetPort: 8024

---
# ============================================================
# 6. PPTX Service
# ============================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: pptx-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pptx-service
  template:
    metadata:
      labels:
        app: pptx-service
    spec:
      containers:
      - name: pptx-service
        image: dokcerregistery.azurecr.io/pptx-service:v1.1
        ports:
        - containerPort: 8025
        resources:
          requests:
            cpu: "100m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: pptx-service
spec:
  type: ClusterIP
  selector:
    app: pptx-service
  ports:
  - port: 8025
    targetPort: 8025

---
# ============================================================
# 7. Web-Scraping Service
# ============================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-scraping-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: web-scraping-service
  template:
    metadata:
      labels:
        app: web-scraping-service
    spec:
      containers:
      - name: web-scraping-service
        image: dokcerregistery.azurecr.io/web-scraping-service:v1.1
        ports:
        - containerPort: 8026
        resources:
          requests:
            cpu: "100m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: web-scraping-service
spec:
  type: ClusterIP
  selector:
    app: web-scraping-service
  ports:
  - port: 8026
    targetPort: 8026

---
# ============================================================
# 8. Word Service
# ============================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: word-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: word-service
  template:
    metadata:
      labels:
        app: word-service
    spec:
      containers:
      - name: word-service
        image: dokcerregistery.azurecr.io/word-service:v1.1
        ports:
        - containerPort: 8027
        resources:
          requests:
            cpu: "100m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: word-service
spec:
  type: ClusterIP
  selector:
    app: word-service
  ports:
  - port: 8027
    targetPort: 8027
